# Protocol fixtures

No captured wire fixtures exist yet. For each future fixture, record build, direction, connection role, capture layer, provenance, sanitization, expected decode and unknown fields. Label synthetic bytes explicitly. Keep original private captures in captures/local, and never call a native memory record a network packet fixture.
