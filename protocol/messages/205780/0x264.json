{
  "name": "ConfiguredSkillRecordLead",
  "build": 205780,
  "buildLabel": "205.780",
  "connectionRole": "game",
  "direction": "server-to-client",
  "messageId": "0x264",
  "representation": "decoded-handler-record",
  "evidenceLevel": "observed",
  "pipeline": {
    "id": "msgpack-schema",
    "state": "hypothesized",
    "opcodeEncoding": "unknown",
    "stages": []
  },
  "sourceReferences": [
    {
      "kind": "note",
      "path": "research/contracts/notes/UI/Widgets/remote-equipped-skills.md",
      "label": "remote-equipped-skills"
    },
    {
      "kind": "note",
      "path": "research/contracts/notes/Protocol/msg-dispatch.md",
      "label": "msg-dispatch"
    }
  ],
  "fields": [
    {
      "id": "field_0x00",
      "offset": 0,
      "kind": "u32",
      "confidence": "unknown",
      "note": "Handler-record slot 0. Native memory, not a wire field."
    },
    {
      "id": "field_0x04",
      "offset": 4,
      "kind": "u32",
      "confidence": "unknown"
    },
    {
      "id": "field_0x08",
      "offset": 8,
      "kind": "u32",
      "confidence": "unknown"
    },
    {
      "id": "field_0x0C",
      "offset": 12,
      "kind": "opaque",
      "rawHex": "00000000",
      "confidence": "unknown",
      "note": "Trailing bytes preserved because their role is not established."
    }
  ],
  "sessionPrerequisites": [
    "A game connection exists and the handler record was produced by the client's own decoder."
  ],
  "wireFixtures": [],
  "unresolved": [
    {
      "question": "What are the wire bytes, opcode encoding and transforms for this message?",
      "kind": "framing",
      "note": "The 12-byte handler record is a decoded native structure. It is not a wire fixture. The transport mechanism is now recovered (see msg-dispatch): the payload leading u32 selects a 32-byte-stride registry record, whose defArray drives a type-directed decode. What remains open is this message's concrete wire bytes -- no capture decodes end to end, and the registry instance is runtime-populated, so id 0x264 is not yet tied to a registry slot."
    },
    {
      "question": "Which build-specific schema variant selects this record's shape?",
      "kind": "schema-variant",
      "note": "The mechanism is established: MsgPackFieldDef descriptor chains with a lazy size cache, keyed by the registry lookup. Not established: this message's own defArray, and whether the registry id space is the same id space this messageId belongs to."
    },
    {
      "question": "Is the registry id space the same namespace as this messageId, and what is its bound?",
      "kind": "identity-mapping",
      "note": "Registry lookup is a direct index in 0..count-1 over a runtime-populated table. count, the table base and every defArray are not yet read, so the two id spaces are not reconciled. Per the working rules these are separate namespaces until shown otherwise."
    }
  ],
  "validation": {
    "static": true,
    "synthetic": false,
    "capturedReplay": false,
    "unity": false,
    "liveInterop": false
  },
  "noClaim": "Records handler-level evidence for one build only. It does not establish wire compatibility, an encoder, or current server support."
}
