namespace Gw2.Contracts;

// Stable numeric skill ids from the native content data. Runtime features may
// consume these identities but must not own their numeric values.
internal enum SkillId : uint
{
    ThrowMine = 30337,
    Aed = 21660,
    RenewedFocus = 9255,
    EffulgentStance = 77095
}
