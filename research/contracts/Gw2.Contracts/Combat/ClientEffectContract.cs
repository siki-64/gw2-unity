namespace Gw2.Contracts;

// Shared client-effect constants. Preview-panel and in-world novelty contracts
// remain separate because their authored definitions are not interchangeable.
internal static class ClientEffectContract
{
    internal const uint ContentTypeEffect = 0xB7;
}
